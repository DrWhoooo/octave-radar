services:
  - type: web
    name: octave-radar-crm
    env: python
    buildCommand: pip install flask
    startCommand: python server.py
    envVars:
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_SERVICE_ROLE_KEY
        sync: false
